{
  "name": "purna-boyapati-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && yarn start",
    "build": "cd frontend && yarn build",
    "start": "cd frontend && yarn start",
    "vercel-build": "cd frontend && yarn install && yarn build"
  },
  "dependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^4.9.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}